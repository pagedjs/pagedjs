{
  "name": "pagedjs",
  "version": "0.5.0-beta.2",
  "description": "Chunks up a document into paged media flows and applies print styles",
  "author": "Fred Chasen",
  "license": "MIT",
  "homepage": "https://pagedmedia.org",
  "main": "src/index.js",
  "type": "module",
  "exports": {
    ".": {
      "import": "./src/index.js",
      "default": "./src/index.js"
    },
    "./polyfill": "./src/polyfill/polyfill.js"
  },
  "dependencies": {
    "clear-cut": "^2.0.2",
    "css-tree": "^1.1.3"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.0",
    "chalk": "^5.6.0",
    "eslint": "^10.0.0",
    "express": "^5.2.0",
    "globals": "^17.0.0",
    "jest-image-snapshot": "^6.5.0",
    "jsdom": "^26.0.0",
    "pdf-to-img": "^5.0.0",
    "playwright-core": "^1.58.0",
    "vite": "^6.4.0",
    "vitest": "^4.1.0"
  },
  "scripts": {
    "build": "vite build && vite build --mode polyfill",
    "start": "vite",
    "test": "npm run lint && vitest run",
    "specs": "npm run build && vitest run --config specs/vitest.config.js",
    "lint": "eslint src specs",
    "docker-build": "docker build -t pagedmedia/pagedjs .",
    "docker-test": "docker build -t pagedmedia/pagedjs . && docker run -it --rm --ipc=host --user pwuser -v $(pwd)/specs:/home/pwuser/pagedjs/specs pagedmedia/pagedjs npm test",
    "docker-specs": "docker build -t pagedmedia/pagedjs . && docker run -it --rm --ipc=host -v $(pwd)/specs:/home/pwuser/pagedjs/specs pagedmedia/pagedjs npm run specs",
    "docker-update-specs": "docker build -t pagedmedia/pagedjs . && docker run -it --rm --ipc=host -v $(pwd)/specs:/home/pwuser/pagedjs/specs pagedmedia/pagedjs npm run specs -- -u"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/pagedjs/pagedjs.git"
  },
  "files": [
    "dist",
    "src"
  ]
}
